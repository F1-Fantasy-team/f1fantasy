import type { Driver } from "../types/driver";

/** Driver list: empty by default. Use API (VITE_API_BASE_URL) or add data locally. Do not commit real mock data. */
export const MOCK_DRIVERS: Driver[] = [];
