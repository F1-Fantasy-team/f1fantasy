import type { Constructor } from "../types/constructor";

/** Constructor list: empty by default. Use API or add data locally. Do not commit real mock data. */
export const MOCK_CONSTRUCTORS: Constructor[] = [];
